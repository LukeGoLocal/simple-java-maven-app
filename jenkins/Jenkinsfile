pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'nvm -B -DskipTests clean package'
            }
        }
        stage('Test') {
            steps {
                sh 'nvm test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Deliver') {
            steps {
                sh './jenkins/scripts/deliver.sh'
            }
        }
        stage('Complete') {
            steps {
                echo 'Job Complete!'
            }
        }
    }
}
